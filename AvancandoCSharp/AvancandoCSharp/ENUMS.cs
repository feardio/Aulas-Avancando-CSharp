using System;
using static System.Console;

namespace AvancandoCSharp
{

    public enum TipoCliente
    {
        PessoaFisica, PessoaJuridica, OrgaoPublico, ONG
    }

    public enum StatusPedido
    {
        AguardandoPagamento = 1,
        Aprovado = 2,
        Enviado = 3,
        Recebido = 4
    }

    class ENUMS
    {
        public void ENUM(string[] args)
        {



            ReadLine();
        }
    }
}
